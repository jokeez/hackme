# Build outputs
hackme.exe
*.exe
!scripts/release/windows/*.exe
resource_windows_amd64.syso
hackme-node
hackme-node-local-treasury
hackme-coordinator
minersign
/bin/
# Root-level `go build -o …` artifacts (never commit; use GitHub Releases / dist/)
/hackme
/coordinator
/workerpoh

# Local Python venv for one-off ops scripts (never commit)
.venv-deploy/

# Local chain state, wallets, settlement — never commit (entire data/ tree)
data/
!data/.gitkeep

# Secrets and operator env (never commit real tokens/seeds)
.secrets/
*.seed
*.seed.hex
**/miner_submit_ed25519_seed.hex
.env
.env.*
!.env.*.example
.env.desktop
.env.worker
telegram_bot.env
.env.telegram

# Logs and desktop runtime
logs/
*.log

# Toolchain caches (desktop installer downloads)
.hackme-toolchains.env
logs/desktop/

# Release artifacts (build in CI / locally; publish via GitHub Releases)
dist/

# Reports and soak artifacts
reports/

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Misc local scratch
cuda-keyring*.deb
/workerpoh-opencl
.cache/
text123.txt
tmp/
GET
Untitled
backups/
*.tar.gz
*.zip
!web/site/**

# Local future-architecture planning (keep local, never push)
adapters/
scripts/ops/adapter_pack_smoke.sh
docs/HUB_ADAPTER_ONBOARDING_CHECKLIST.md
docs/HUB_ADAPTER_STATUS_TEMPLATE.md
