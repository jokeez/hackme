# HTTP API

**English (integrators):** Default base URL is the server root (e.g. `http://127.0.0.1:8080`). When **`HACKME_ADMIN_TOKEN`** is set, mutating **POST** routes require `X-Hackme-Admin-Token` or `Authorization: Bearer …`. Main read paths: **`GET /`** dashboard, **`GET /api/status`** (chain, economics, `network_sync`, `miner_public_ux`), **`GET /api/metrics`** (node telemetry + mining/econ fields), **`GET /api/global/metrics`** (pool-wide KPIs), **`GET /api/worker/status`**, **`GET /api/worker/settlement`**, **`GET /api/wallet`**, **`GET /api/wallet/earnings`**, **`GET /api/chain`**, **`GET /api/reports/blocks`**. Pool worker: **`POST /api/worker/start`** / **`POST /api/worker/stop`** (admin). Genesis: **`POST /api/genesis`**. Orders: **`POST /api/tasks`**, **`POST /api/tasks/from_code`**. Full tables below are kept in sync with the implementation.

Base prefix: server root (default `http://127.0.0.1:8080`). All JSON responses with `Content-Type: application/json` where indicated.

## Mutation authentication

Production defaults expect **`HACKME_ADMIN_TOKEN`** set and **`HACKME_REQUIRE_ADMIN_TOKEN=1`**. Mutating **POST** routes use **`requireAdminAuthStrict`** (fail-closed if the token is unset). Without a matching header they return **401** and **`WWW-Authenticate: Bearer realm="hackme-admin"`**.

Pass the token in one of the following ways:

- **`X-Hackme-Admin-Token: <token>`**
- **`Authorization: Bearer <token>`**

Lab-only: `HACKME_REQUIRE_ADMIN_TOKEN=0` on a **loopback** bind. Do not open a public bind without a token. Threat model: **`docs/SECURITY.md`**.

## Statics and page

| Method | Path | Description |
|--------|------|----------|
| GET | `/` | HTML dashboard |

## Metrics and chain

| Method | Path | Description |
|--------|------|----------|
| GET | `/api/global/metrics` | Canonical unit for pool-UI: `{ ok, global_source, sample_ts, stale_sec, chain, network, work }`. `chain` contains `has_genesis/tip_height/tip_hash/total_blocks`; `network` - global miners/TH/s/rigs with a source attribute; `work` – coordinator counters (`issued_ranges`, `submitted_items`, `accepted_attempts`, `target_mod`, `workers_count`). Use this endpoint for general KPIs for all miners (GLOBAL), and the local `GET /api/metrics` - only for LOCAL telemetry. |
| GET | `/api/metrics` | CPU, RAM, disk, network, GPU (**temperature/utilization**: NVIDIA via `nvidia-smi`; **AMD on Linux** - sysfs amdgpu, `temp*_input` / `gpu_busy_percent` / if available `power1_average`); **CPU temp** (OS sensors, best-effort); load avg (Unix); `block_height`; `mining_*`, `mining_load_pct` at PoH; **`mining_poh_backend`**: `cpu` \| `cuda` \| `opencl` \| `mixed` (builds `-tags cuda` and/or `-tags opencl` if the GPU path is successful); **`mining_gpu_devices`**, **`mining_gpu_total_gh_s`** - by card with multi-GPU PoH; **`mining_rigs`**: with GPU - lines `local-gpu` for each card, otherwise one local rig + deleted from `POST /api/push_work`. **Economy:** `econ_max_supply_hmc`, `econ_total_minted_hmc`, `econ_total_burned_hmc`, `econ_circulating_hmc`, `econ_mint_remaining_hmc`, `econ_order_burn_rate`, `econ_burn_impact_pct`, reward plan `econ_base_reward_now_hmc`, `econ_reward_tail_floor_hmc`, `econ_reward_halving_interval_blocks`, `econ_next_halving_block`, `econ_expected_empty_hmc_hour`, as well as the actual 1h window through `econ_window_*` (base/order/total + share). **Retarget/capacity:** `mining_target_mod_cap`, `mining_target_mod_at_cap`, `mining_observed_block_sec`, `mining_target_block_sec`. **Mining insight (heuristics):** `mining_eta_sec_est` (−1 if no data), `mining_eta_progress` (0...~1), `mining_poh_blocks_last_1h`, `mining_hmc_last_hour_approx` (actual base+order estimate for window blocks), `mining_projected_hmc_hour` (from current reward and ETA), `mining_insight_note` |
| GET | `/api/status` | `chain_id`, `has_genesis`, `tip_height`, `tip_hash`, `mining`, `node_address`, **`schema_version`**, **`schema_expected`**, **`admin_auth_enabled`** (bool: whether specified `HACKME_ADMIN_TOKEN`), **`network_mode_active`**, **`pool_coordinator_url`** (from env), **`pool_coordinator_url_effective`** (env or worker session URL), `economics` (`max_supply_hmc`, `total_minted_hmc`, `total_burned_hmc`, `circulating_hmc`, `mint_remaining_hmc`, `burn_rate_order`, `order_fee_rate`, `network_fee_burn_share`, `network_fee_dev_share`, `dev_fee_address`, `policy_hash`), **`network_sync`** (open network/follower): `p2p_enabled`, **`public_authority_base`** (env `HACKME_PUBLIC_AUTHORITY_BASE`, if set - one VPS as a canon/pool source: at startup substitutes `HACKME_CANONICAL_CHAIN_URL` and, if necessary, `HACKME_POOL_COORDINATOR_URL`), **`p2p_sync_hint`** (as `GET /api/p2p/sync`: `local_height`, `max_peer_height`, `lag_blocks`, `sync_needed`, `best_peer_url`, `best_peer_node`), `canonical_ledger_lag_blocks` (canonical tip − local `tip_height`, if the canon is known), `state_replay_enabled` (mirror `HACKME_P2P_SYNC_STATE_REPLAY_ENABLED`), `background_sync_interval_sec` (`HACKME_P2P_BACKGROUND_SYNC_SEC`), `bind_addr` (env `HACKME_BIND_ADDR`), `http_cors_configured` (whether `HACKME_HTTP_CORS_ALLOW_ORIGIN` is specified), **`miner_public_ux`**: `coordinator_url_effective`, `canonical_chain_base`, `canonical_remote_reachable`, `p2p_peers_configured`, `worker_subprocess_running`, **`hints_en`** (tips for public miners in English), `sandbox_policy` (`locked`, `profile`, `max_check_wasm_bytes`, `check_timeout_ms`, `compiled_cache_max`, `memory_limit_pages`, `block_quarantined`), `crypto_policy` (`block_sig_algs_supported`, `transfer_sig_algs_supported`, `pq_ready_wire_versioning`), `consensus_policy` (`simultaneous_block_rule`, `fork_resolution`, `fork_action`). **Pool / VPS (read-only UI):** `pool_target_mod`, `pool_target_mod_source` (`coordinator` / `canonical_metrics`), `pool_workers_count`, `pool_global_hashrate_th_s`, `pool_total_miners`; for errors - `pool_work_stats_error` / `pool_network_stats_error` |
| GET | `/api/wallet` | `address`, `balance_hmc` |
| GET | `/api/wallet/earnings?window_hours=24&bucket_sec=3600&address=HMC-…` | Wallet movement units and timebuckets: `{ ok, source, data }`, where `data` contains `address`, `window_hours`, `bucket_sec`, `now_unix`, `total_received_hmc`, `total_sent_hmc`, `total_net_hmc`, `received_24h_hmc`, `sent_24h_hmc`, `net_24h_hmc`, `settled_out_24h_hmc`, `tx_count_24h`, `settled_out_window_hmc`, `tx_count_window`, `buckets[]`. Each bucket: `{ bucket_unix, received_hmc, sent_hmc, net_hmc, settled_out_hmc, tx_count }`. Parameters are limited by the server: `window_hours` up to 90 days, `bucket_sec` from 300 to 86400. Optional **`address`** - form `HMC-` + 16 hex; if not available, the node's wallet is used. Remote nodes with `HACKME_CANONICAL_CHAIN_URL` proxy the request with the same `address` so that the public command node calculates metrics based on your address, and not on the VPS wallet. |
| GET | `/api/chain?limit=N` | `blocks`: array of JSON blocks (old → new within the limit). Each block contains `miner_sig_alg` (now `ed25519`), `miner_pubkey_ed25519` and `miner_sig_ed25519` (signed `hash` by node key). |
| GET | `/api/reports/blocks?limit=N&source=…` | Brief summary of units **new first**: `blocks`, `note`, **`blocks_source`** (`local_ledger` or `canonical_peer`), **`local_tip_height`**, optional **`canonical_tip_height`**, **`canonical_base`**. **`source`** parameter: `auto` (default) - in **network mode**, if canon height ≠ local SQLite, the server **proxy** list from node `HACKME_CANONICAL_CHAIN_URL`; otherwise local database. `local` - always local. `canonical` - forced from canon (if an error occurs, rollback to a local table). Default limit 30, max. 500 |

## Genesis

| Method | Path | Description |
|--------|------|----------|
| POST | `/api/genesis` | Create block #0 and initialize the chain. One-time mint **50000 HMC** to consensus treasury address `DevFeeAddress` (see `economics` in `/api/status`); the line `wallet` id=1 remains the node address (miner in the block header). **409** if genesis already exists. **401** if `HACKME_ADMIN_TOKEN` is specified and there is no token header. Response body: `block`, `balance` (genis mint size in HMC), `chain_id` (inside `block`: `miner_sig_alg`, `miner_pubkey_ed25519`, `miner_sig_ed25519`). |

## Mining (WASM PoH demo)

| Method | Path | Description |
|--------|------|----------|
| POST | `/api/mining/start` | Local WASM PoH only on **command-node** with `HACKME_CHAIN_LEADER_LOCAL_POH=1` (need genesis). Otherwise **409** `local_poh_disabled` / `local_poh_disabled_in_network_mode` - pool members use `POST /api/worker/start`. **412** without genesis. **401** without admin secret |
| POST | `/api/mining/stop` | Stop searching. **401** with active `HACKME_ADMIN_TOKEN` without secret |
| POST | `/api/worker/start` | Launching the outer worker loop (`scripts/ops/worker_loop.sh`) to the coordinator. JSON body optional: `coord_url`, `coord_token`, `worker_id`, `batch_size`, `hashrate_gh_s`. You need a coordinator token: **`HACKME_POOL_COORDINATOR_TOKEN`**, or **`HACKME_ADMIN_TOKEN`** on the node, or the admin header when requesting, or `coord_token` in JSON. **401** without admin token; **412** if the coordinator URL could not be displayed |
| POST | `/api/worker/stop` | Forcefully stop the worker's child process (**SIGKILL** on PID). **401** without admin |
| GET | `/api/worker/status` | `{ ok, running, pid?, coord_url, worker_id, batch_size, hashrate_gh_s, log_path }` - read only, no admin |
| GET | `/api/worker/settlement` | Summary of worker accruals (coordinator accrual) vs locally marked settlement state: `{ ok, source, workers_count, total_accrued_hmc, total_settled_hmc, total_unpaid_hmc, min_settle_hmc, daily_min_settle_hmc, daily_force_interval_sec, last_force_unix, next_daily_sweep_unix, daily_sweep_eta_sec, threshold_ready, state_file, updated_unix }`. Takes `workers[*].payout_hmc` from `/api/work/stats?details=1` and compares it with the state file (`HACKME_WORKER_SETTLEMENT_STATE_FILE` / `WORKER_SETTLEMENT_STATE_FILE` / `data/worker_settlement_state.json`). SLA fields help UI/monitoring show the expected window of the nearest daily sweep. **Public nginx:** include this GET in the allowlist (see `scripts/ops/nginx/hackme-site-domain.tls.conf`, path `worker/settlement`), otherwise external gates will receive **403**. |
| GET | `/api/mining/logs` | `{ "lines":[...], "mode":"empty|miner|worker|both", "worker_running", "worker_log_path", ... }` — local WASM miner buffer; with **`?include_worker=1`** and header **admin** a tail (up to 120 lines) of **`worker_log_path`** (stdout/stderr pool worker) is added so that the Session log is not empty without local PoH |
| GET | `/api/mining/logs/stream` | **Server-Sent Events** (only while **`mining` is active**; otherwise **400**). First the event **`snapshot`** with a JSON array of strings (the current buffer), then messages with the body `{"line":"..."}` for each new line; comments `: ping` for keep-alive. Dashboard uses flow with rollback to polling `GET /api/mining/logs` |
| GET | `/api/hardware/tune` | CPU: Current Soft Ceiling **`soft_cap_pct`**, Hints, Range 1-100%. GPU: **NVIDIA** - temperature/utilization/limits from `nvidia-smi`; **AMD (Linux)** - temperature/load/power from sysfs amdgpu (`amd_telemetry`). Field `env`: **`HACKME_GPU_TEMP_*`**. For UI: `can_set_power_limit` (**NVIDIA only** for `nvidia-smi -pl`) + `power_limit_hint`. Alias: **`GET /api/gpu/tune`** |
| POST | `/api/hardware/tune` | One request - **either** `{ "soft_cap_pct": 75 }` (soft CPU limit for miner, **401** with admin), **or** `{ "gpu_index": 0, "power_limit_w": 220 }` → **`nvidia-smi -pl`** (only with NVIDIA; on AMD-only **400**). Do not mix fields in one body (**400**). If errors occur, the GPU may return `code="insufficient_permissions"` + `hint`. Alias: **`POST /api/gpu/tune`** |

## Orders (SQLite `tasks`)

| Method | Path | Description |
|--------|------|----------|
| POST | `/api/tasks` | Upload the order manifest (JSON in the body). **401** with active `HACKME_ADMIN_TOKEN` without secret; **429** with burst/rate-limit. Fields: **`id`** (unique), **`kind`**: `synthetic_poh_v1`, **`reward_hmc`** &gt; 0, optional **`difficulty_score`** (1..100, default 1), optional **`target_solves`** (default 1, maximum 10000), optional **`ttl_sec`** (manual TTL; if not specified, calculated automatically), optional **`payer_ref`** (customer ID, up to 256 runes). Fairness-guard: **`reward_hmc >= difficulty_score × 0.0005`**. WASM-gate: either **`wasm_check_hex`** (module hex) or **`wasm_artifact_path`** (relative path to `.wasm` under **`tasks/artifacts/`** or **`HACKME_TASK_ARTIFACT_DIR`**) **and** **`artifact_hash`** (SHA-256 file, 64 hex); not both at once. The following is debited from the node's wallet: **`prepaid_hmc = reward_hmc × target_solves`** + **`order_fee_hmc = prepaid_hmc × 0.05`**; There is an hourly cap `HACKME_MAX_ORDER_ESCROW_PER_HOUR_HMC` (for the prepaid part). Success **200**: `{ "ok", "id", "payer_ref", "prepaid_hmc", "order_fee_hmc", "total_debit_hmc", "order_burn_hmc", "balance_after", "expires_at", "ttl_sec", "signature_ed25519", "signing_public_key_ed25519", "verified_by", "verification_receipt" }` - signature is calculated based on **exact bytes of the request body**. `verification_receipt` contains `pool_id`, `request_sha256`, `issued_at_unix` and the receipt-payload pool signature. **400** — validation error; **402** (`Payment Required`) - not enough HMC on the wallet. |
| POST | `/api/tasks/from_code` | Create an order from source code (admin-only). Body: `{ "id"?, "language": "<lang>", "code", "reward_hmc", "difficulty_score", "target_solves", "payer_ref"? }`. Languages: **`rust`**, **`c`**, **`cpp`**, **`tinygo`**, **`zig`**, **`assemblyscript`**, **`wat`**. Aliases: `rs→rust`, `gcc→c`, `c++/cxx/cc→cpp`, `go/golang→tinygo`, `as→assemblyscript`, `asm/assembly/wast/wasm-text→wat`. The node must have nodes in the **`PATH`** process: `rustc` (often rustup), **`clang`** (c/cpp), **`tinygo`**, **`zig`** (zig), **`asc`** (AssemblyScript, usually `npm i -g assemblyscript`), **`wat2wasm`**. Compilation under **bwrap/nsjail** if possible; without them - **host compile (lab-only)**. Gate **`HACKME_FROM_CODE`** (`0` off; unset=loopback only). Cont: **`HACKME_FROM_CODE=0`**; with compile - **`HACKME_FROM_CODE_REQUIRE_SANDBOX=1`** and narrow bwrap/nsjail (without `--ro-bind / /`). For **tinygo**, **zig** and **assemblyscript**, after compilation, sanitize is performed (only the function export **`check`** remains, the start section is removed), then the same strict ABI-gate (`check(i64)->i32`, exactly one function-export). Errors: `compile_failed`, `wasm_validation_failed`, `wasm_sanitize_failed`, `manifest_rejected`. For VPS, see `scripts/ops/install_vps_from_code_toolchains.sh`. |
| GET | `/api/tasks` | Order list: `{ "tasks": [ { "id", "artifact_hash", "reward", "difficulty_score", "status", "created_at", "kind", "target_solves", "progress_count", "progress_pct", "payer_ref", "prepaid_hmc", "expires_at", "cancelled_at", "cancel_reason", "refunded_hmc" } ] }`. Statuses: `open`, `completed`, `cancelled`. When the TTL auto-expires, the open order is transferred to `cancelled` with `cancel_reason="timeout"` and the unused escrow is returned to the wallet (`refunded_hmc`); solve rewards already paid to miners are not rolled back. If there is an open order with `reward_hmc > 0`, the miner takes it **before** synthetics and `./tasks/*.json` (FIFO by `created_at`). Progress increases with PoH blocks with `order_task_id` in the payload of the block task |
| POST | `/api/poh/solve-order` | Admin-only pool→chain relay. Body: `{ "miner_address", "found_nonce", "target_mod", "order_task_id" }`. Chain **forces canonical** `poh_target_mod` (ignores caller M for payout), validates WASM gate, appends PoH block, credits order escrow. Used by coordinator after a valid found submit. **401** without admin token. |

## Fuzz Campaigns (v1)

| Method | Path | Description |
|--------|------|----------|
| GET | `/api/fuzz/campaigns?limit=N` | List of campaigns (fuzz/property/symbolic). Answer: `{ ok, campaigns[], count }`. |
| POST | `/api/fuzz/campaigns` | Create a campaign (admin-only). Body: `{ id?, campaign_type:"fuzz|property|symbolic", status?, title?, description?, owner_ref?, task_id?, target_ref?, budget_runs?, budget_seconds?, config? }`. In the one-time response, the viewing secret is: `customer_report_token` (pass it on to the customer), only the hash is stored on the server side. |
| POST | `/api/fuzz/housekeeping` | Admin-only global sweep for all campaigns: `{ max_findings?, max_corpus?, max_runtime_samples? }` — bounded cleanup for `fuzz_findings` / `fuzz_corpus` / `fuzz_runtime_samples` + file cleanup in `HACKME_FUZZ_ARTIFACT_DIR` by TTL/size policy. |
| POST | `/api/fuzz/artifacts/cleanup` | Admin-only cleanup of artifact files: `{ ttl_sec?, max_bytes? }`. Deletes expired files (by modtime) and additionally oldest-first if the total directory size is > `max_bytes`. |
| GET | `/api/fuzz/campaigns/{id}` | Campaign card. |
| POST | `/api/fuzz/campaigns/{id}/status` | Update campaign status (admin-only): `{ status:"planned|running|paused|completed|cancelled", summary? }`. |
| POST | `/api/fuzz/campaigns/{id}/runtime` | Runtime heartbeat/update (admin-only): `{ status?, runs_done?, new_edges?, new_paths?, unique_crashes?, time_to_first_crash_sec?, summary? }`. Auto-logic: `planned/paused -> running`; at `runs_done >= budget_runs` the campaign closes at `completed`. For offline mode, the server itself can tick runtime in the background (`HACKME_FUZZ_AUTORUN=1`, `HACKME_FUZZ_AUTORUN_TICK_SEC`, per-campaign opt-out `config.auto_runner=false`, step `config.autorun_runs_step`). |
| GET | `/api/fuzz/campaigns/{id}/runtime/history?limit=N` | Runtime samples history (token/admin): `{ ok, campaign_id, count, samples[] }`, where `samples[]` contains `sampled_at,status,runs_done,new_edges,new_paths,unique_crashes,heartbeat_at`. |
| POST | `/api/fuzz/campaigns/{id}/findings` | Download findings (admin-only), single/batch: `{ finding }` or `{ findings:[...] }`. Ingest is duplicate-resistant: it counts `duplicates_id` (repeat id) and `duplicates_natural` (match `finding_type+input_sha256+title`). Returns an updated `summary` campaign. |
| POST | `/api/fuzz/campaigns/{id}/token` | Rotation of customer report token (admin-only). The answer again contains one-time `customer_report_token`. |
| GET | `/api/fuzz/campaigns/{id}/access?limit=N` | Admin-only audit trail of report readings: `{ ok, campaign_id, count, events[] }`, where `events[]` contains `actor_type` (`admin|customer`), `access_kind` (`report_json|report_csv|gate|pulse|corpus|crashes|runtime_history`), `remote_ip`, `user_agent`, `accessed_at`. |
| GET | `/api/fuzz/campaigns/{id}/access/summary?window_sec=86400` | Admin-only aggregated access statistics: `{ ok, campaign_id, window_sec, window_since, window_until, total, last_access_at, by_actor, by_access_kind, actor_kind_matrix }`. |
| GET | `/api/fuzz/campaigns/{id}/corpus?limit=N` | Corpus snapshot (token/admin): `{ ok, campaign_id, count, corpus[] }`, where the corpus element includes `input_sha256`, `hits`, `first_seen_at`, `last_seen_at`, `last_finding_id`, `artifact_path`. |
| POST | `/api/fuzz/campaigns/{id}/corpus/retention` | Admin-only cleanup: `{ max_items }` — leaves top-N corpus inputs to `hits,last_seen`; answer `{ ok, deleted }`. |
| POST | `/api/fuzz/campaigns/{id}/housekeeping` | Admin-only bounded campaign cleanup: `{ max_findings?, max_corpus?, max_runtime_samples? }` - cuts off the tails of `fuzz_findings` / `fuzz_corpus` / `fuzz_runtime_samples` by retention limits. |
| GET | `/api/fuzz/campaigns/{id}/crashes?limit=N` | Crash triage view (token/admin): `{ ok, campaign_id, count, crashes[] }` (findings where `finding_type` is similar to crash or severity = critical/high). |
| GET | `/api/fuzz/campaigns/{id}/report?limit=N` | Unified customer report. **Default HTML**. JSON: `?format=json` or `Accept: application/json`. Fields: `{ ok, report_version:"fuzz_report_v2", human_summary, verdict_card, gate{pass,reasons}, assurance_note, target_fingerprint, baseline_diff, security_summary{…,triage_policy:"crash_first"}, top_issues[] (crash/hang/ASan/memory only), coverage_noise[] (detector appendix), recommendations[], totals, findings[] }`. Same HTML: `GET …/report.html`. Auth: `X-Hackme-Report-Token` or admin. |
| GET | `/api/fuzz/campaigns/{id}/report.csv?limit=N` | CSV export: executive summary + crash-first top issues + coverage noise. Auth: report token or admin. |
| GET | `/api/fuzz/campaigns/{id}/gate?max_critical=0&max_high=0&max_severity_score=0&min_sample_size=0&min_runs_done=0` | CI/release gate (**primary deliverable**). `{ pass, reasons[], assurance_note, triage_policy:"crash_first", thresholds{severity_basis:"crash_class"}, observed{critical_count,high_count,severity_score,crash_count,coverage_noise_count,runs_done} }`. Thresholds apply to **crash-class** findings only; detector/property signals do not fail the gate. Default `min_sample_size=0` so clean (zero-finding) campaigns can PASS; prefer `min_runs_done` for assurance. `pass` ≠ proven secure. Auth: report token or admin. |
| GET | `/api/fuzz/campaigns/{id}/pulse?stale_after_sec=90` | Live progress: `{ ok, status, elapsed_sec, progress{runs_done,budget_runs,progress_pct,runs_per_sec,eta_sec,eta_sec_est,eta_source,progress_note,honest_progress}, eta{…}, runner, coverage, findings{total,by_severity,crash_count,coverage_noise_count}, summary, escrow? }`. Auth: report token or admin. |
| GET | `/api/fuzz/campaigns/{id}/diff?base_campaign_id=<id>` | Regression diff of two campaigns (admin-only): `{ ok, campaign_id, base_campaign_id, summary{new_count,fixed_count,regressed_count}, new_findings[], fixed_findings[], regressed_findings[] }`. |

## HMC transfers (transfer v1)

| Method | Path | Description |
|--------|------|----------|
| POST | `/api/tx/send` | Sending a signed transfer transaction. Body: `{ "tx_type":"transfer_v1", "sig_alg":"ed25519"?, "from", "to", "amount_units", "fee_units", "nonce", "timestamp_unix", "memo"?, "pubkey_ed25519", "sig_ed25519" }`, where `amount_units/fee_units` are the minimum units of **Kapa**. If `sig_alg` is not specified, `ed25519` is assumed; any other algorithms are now rejected as `unsupported_sig_alg` (fail-closed wire versioning under future PQ). When the block is turned on, `fee_units` is distributed as: **30% burn**, **70% dev/service fee** on **hardcoded** `DevFeeAddress` (`HMC-719006d93916ad52`, consensus policy, without runtime override). Success: `{ "ok": true, "tx_hash", "status": "pending" }`. Errors in `{ "error", "code" }` format. Exchange integration pack: **`docs/EXCHANGE_LISTING_WALLET_PREP.md`**. |
| GET | `/api/tx/{hash}` | Transaction status: `pending` / `included` / `rejected`, plus block fields upon confirmation. |
| GET | `/api/address/{address}` | Address status: `{ "address", "balance_units", "next_nonce" }`, where `balance_units` is in **Kapa**. |
| GET | `/api/tx/pool` | Current mempool (debug/admin): list of pending tx with sorting fields. |

## P2P (private testnet baseline)

| Method | Path | Description |
|--------|------|----------|
| POST/GET | `/api/p2p/handshake` | Exchange `node_id`, `height`, `tip_hash`, `seen_at`, `announce_url`, `policy_hash`. In closed mode, economic-policy mismatch results in `403 p2p policy mismatch` (peer rejected). With `HACKME_P2P_DISCOVERY=1` enabled, the node can automatically add a new peer from `announce_url` after a valid handshake. |
| POST | `/api/p2p/tx` | Accepting gossip transfer transactions; local validation and attempt to add to `tx_pool`. If successful, the node relays the tx further via p2p with anti-loop dedup (TTL cache) to avoid storms. |
| GET | `/api/p2p/peers` | Snapshot of known peers and health status. The answer is `{ enabled, peers[] }`, where for each peer there are `peer_url`, `node_id`, `height`, `tip_hash`, `seen_at`, `healthy`, `last_attempt_unix`, `last_ok_unix`, `next_retry_unix`, `last_latency_ms`, `consecutive_failures`, `flap_count`, `last_state_change_unix`, `unstable`, `quality_score`, `quality`, `last_error`. |
| GET | `/api/p2p/sync` | Snapshot sync by title: `{ enabled, local_height, local_tip, max_peer_height, lag_blocks, sync_needed, best_peer_url, best_peer_node, pull_preview, sync_blocked, sync_blocked_code, sync_action }`. `pull_preview` shows linear-tail plan (`plan_ready`, `planned_from_height`, `planned_to_height`, `planned_count`, `hashes[]`) with depth-limit (`depth_limit`, query param `depth_limit=N`) without applying blocks to the local DB. For a stale/lag situation without a healthy peer, soft-wait (`sync_blocked=false`, `sync_blocked_code=sync_waiting_peer_freshness`) is returned instead of hard-fail. |
| POST | `/api/p2p/sync/pull?depth_limit=N` | Admin-only. Performs a linear pull on `pull_preview` and saves blocks to the staging table `p2p_sync_stage` (`staging_only` mode, without changing the canonical chain). |
| GET | `/api/p2p/sync/stage?limit=N` | List of staged blocks from `p2p_sync_stage` for the operator. |
| POST | `/api/p2p/sync/apply?max_apply=N` | Admin-only. Default **disabled (fail-closed)**: returns `code="sync_apply_disabled_no_state_replay_v1"` to exclude chain/state drift. For controlled-mode, enable `HACKME_P2P_SYNC_STATE_REPLAY_ENABLED=1`. In enabled mode, it uses contiguous linear tail from `p2p_sync_stage` with `max_apply` (default 20, max 200), while staged blocks pass hash+signature check (if signature fields are present). |
| POST | `/api/p2p/sync/run?depth_limit=N&max_apply=M` | Admin-only one-shot pull+apply. If apply is disabled by policy, returns `code="sync_apply_disabled_no_state_replay_v1"`. If fork (`no_direct_tail_match`) is detected, returns `{ ok:false, code:"fork_detected_no_reorg_v1", action:"stop_mining_on_follower_and_reseed_db_from_leader" }`. |

If `HACKME_P2P_TOKEN` is specified, `/api/p2p/*` requires the header `X-Hackme-P2P-Token`.

## Pool / “hive” (LAN coordinator + step to P2P)

| Method | Path | Description |
|--------|------|----------|
| GET | `/api/network/stats` | **Default - LAN + command node unit:** sum of GH/s of remote workers (online &lt;45 s) + local PoH (as in metrics), `global_hashrate_th_s` in TH/s, `peer_connections` = number of online workers, `top_miners` - top by GH/s (supplemented by placeholder slots to 5), `active_rigs` — lines from `push_work`, `global_mock: false`. **`HACKME_NETWORK_MOCK=1`** - old random “global” layer + simulated rigs with an empty pool, `global_mock: true`. If **`HACKME_POOL_COORDINATOR_URL`** is given, the command node will proxy this GET to the coordinator and return it in JSON (fallback to the local aggregate on coordinator error). |
| POST | `/api/push_work` | The external worker sends heartbeat/share: JSON `{ "worker_id", "name"?, "hashrate_gh_s"?, "ip"?, "share_accepted"?: bool, "shares_accepted"?: uint }`. **401** with active `HACKME_ADMIN_TOKEN` without secret. The default IP is from `RemoteAddr`. The row is stored in SQLite **`lan_peer_rigs`**. If **`HACKME_POOL_COORDINATOR_URL`** is specified, heartbeat is additionally forwarded to coordinator (best-effort). For a protected coordinator, use **`HACKME_POOL_COORDINATOR_TOKEN`**. The answer is `{ "ok": true, "worker_id": "..." }`. |
| POST | `/api/work/claim` | (Coordinator) Issuing the range `nonce` to the worker: `{ "worker_id", "batch_size"? }` → `{ "base_nonce", "batch_size", "target_mod", "lease_expires_at", "work_id", "chunk_id", "scheduler_mode", "task_class" }`. `scheduler_mode` = `orders|baseline`; `task_class` reflects the current job class (strict orders-first). `work_id` is recommended to be passed back to submit to prevent range confusion. With the coordinator token - **401** without a secret. Anti-abuse: **429** + `reason` (`claim_rate_limited`, `worker_temporarily_banned`, `too_many_workers`, `too_many_active_leases`) when exceeding pool limits/limits. |
| POST | `/api/work/submit` | (Coordinator) Range report: `{ "worker_id", "base_nonce", "batch_size", "work_id"?, "attempts"?, "found"?, "found_nonce"?, "result_hash"?, "proof_hash"?, "hashrate_gh_s"?, "miner_address"?, "miner_pubkey_ed25519"?, "miner_sig_ed25519"?, "miner_sig_alg"?, "submit_nonce"? }` → `{ "ok", "accepted", "reason", "payout_hmc" }`. Accrual: for `attempts` (or `batch_size`, if `attempts` is not transferred) at the rate `HACKME_COORDINATOR_REWARD_PER_M_ATTEMPTS` + bonus `HACKME_COORDINATOR_FOUND_BONUS_HMC` at `found=true`; `attempts` is clamped to `batch_size`. Deduplication `result_hash` prevents you from repeatedly paying for the same useful result. When `HACKME_POOL_HYBRID_SIGNER_ENABLED=1` submit, the signature is optional for backward compatibility, but if v2 signature fields are submitted, they are strictly validated (including replay check according to `submit_nonce`). If the lease is expired/foreign/unknown - **409** and `reason` (`lease_expired`, `range_leased_to_another_worker`, `unknown_or_already_closed_range`, `work_id_mismatch`, `duplicate_result_hash`, `invalid_signature`, `invalid_pubkey`, `pubkey_address_mismatch`, `missing_signature_fields`, `replay`). Anti-abuse: **429** + `reason` (`submit_rate_limited`, `worker_temporarily_banned`); repeated bad submissions increase strikes. |
| GET | `/api/work/stats` | (Coordinator) Issue/submit counters: `next_nonce`, `issued_ranges`, `reissued_ranges`, `submitted_items`, `found_hits`, `expired_leases`, `unknown_submits`, `stale_submits`, `rejected_submits`, `dedup_submits`, `accepted_attempts`, `total_payout_hmc`, as well as `default_batch`, `target_mod` (**actually applied** M after clamp), `target_mod_min`, `target_mod_max` (bounds from env `HACKME_COORDINATOR_POOL_TARGET_MOD_MIN` / `HACKME_COORDINATOR_POOL_TARGET_MOD_MAX`), `target_mod_load_hint` (information assessment of the “desired” M by pool load **without** clamp - only for UI/monitoring), `target_mod_load_capped` (`true`, if the hint is higher than `max` and a truncated M is actually used), `lease_sec`, `reward_per_m`, `found_bonus_hmc`, `claim_per_min`, `submit_per_min`, `bad_strikes_to_ban`, `ban_sec`. For hybrid v2 added: `hybrid_signer_enabled`, `hybrid_signer_strict`, `hybrid_require_found_sig`, `signed_submits_accepted`, `signed_submits_rejected`, `last_signed_miner_address`. In `workers` (with `?details=1`) each worker can contain `payout_address` and `signed_submits` - this is the address/counter of valid signed submissions, used by payout-settlement scripts. For zoom/observability: `workers_count`, `active_leases_count`, `abuse_count`, `max_workers`, `max_active_leases`, `max_dedup_entries`, `ingress_q_len`, `drop_reason_count`, `ack_latency_ms`, `scheduler_mode`, `scheduler_transitions`, `orders_active`, `orders_probe_unix`. Detailed cards `active_leases/workers/abuse` are returned only at `?details=1` (or `true/yes`) to avoid inflating the response on large pools. On the command node, the endpoint is also available as a proxy to the coordinator when `HACKME_POOL_COORDINATOR_URL` is specified; if coordinator is not available, JSON with `ok:false` and `reason` is returned. |
| GET/POST | `/api/mining/devices` | Inventory and settings of mining devices. `GET` returns `{ "cpu_model", "gpu_count", "gpu_active", "profile_mode", "gpu_devices":[{ "index","backend","name","default_label","alias","display_name","enabled","priority" }] }`. `POST` (admin) accepts either `{ "profile_mode":"cpu|gpu|mixed" }` or `{ "backend","index","alias","enabled","priority" }` to store GPU alias/activation/priority. |

## Error codes

- **401** - `HACKME_ADMIN_TOKEN` was specified, but the request did not contain a valid secret (see section “Mutation Authentication”).
- **409** — reinitialization of genesis.
- **412** — mining without genesis (`Precondition Failed`).
- **402** — `POST /api/tasks`: Insufficient balance for escrow order.
- **405** - invalid HTTP method.
- **400** — invalid transfer transaction; field `code` contains the reason (`invalid_signature`, `invalid_nonce`, `insufficient_balance`, etc.).
- **400** — invalid transfer transaction; field `code` contains the reason (`invalid_signature`, `invalid_nonce`, `insufficient_balance`, `pending_nonce_conflict`, etc.).
- **429** — local rate-limit (`/api/tx/send`, `/api/tasks`, `/api/p2p/tx`, `/api/push_work`) or anti-abuse limit coordinator was triggered.
