# Vast.ai Ultra Test: 1× RTX 4090

Date: 2026-06-02  
Host: `23.158.136.85:31117` (Vast instance `e70ecc07cdf3`, geo ~Tokyo JP)

SSH:

```bash
ssh -p 31117 root@23.158.136.85 -L 8080:localhost:8080
# or proxy: ssh -p 24171 root@ssh2.vast.ai
```

## Scope

- Full inventory + regional latency probe
- 6-minute production mining run (`RUN_SECONDS=360`)
- CUDA path preflight; OpenCL fallback if NVRTC incompatible
- Coordinator validation: online, GH/s, attempts/hits, payout, `target_mod` drift
- Local fuzz smoke: `scripts/ops/security_audit_gate.sh` (clean verdict)

## Inventory

| Item | Value |
|------|--------|
| GPU | 1× NVIDIA GeForce RTX 4090 |
| Driver | 580.142, CC 8.9 |
| VRAM | 49140 MiB reported |
| CPU/RAM | 256 vCPU / 629 GiB |
| Pack | `vast-gpu-matrix-20260602T151608Z.tar.gz` |
| Worker ID | `vast-4090-01` |

## Network

- ping `hackme.tech`: **~238 ms** avg (high RTT via CF edge; instance egress ~156.243.244.210 Tokyo)
- Coordinator `/health` TTFB ~1.1 s, `/api/work/stats` ~1.3 s
- Mining remained stable despite latency (120 signed submits, 0× HTTP 403)

## Runtime

### CUDA (failed — expected on CUDA 13 hosts)

- `ldd`: `libnvrtc.so.12 => not found`
- Symlink `libnvrtc.so.13` → `.12` insufficient: `version 'libnvrtc.so.12' not found`
- **Action for final pack:** ship NVRTC/CUDA13-compatible `workerpoh-cuda` or preflight + auto OpenCL

### OpenCL (PASS — max throughput on this host)

```
workerpoh: searcher=GPU #0 [OpenCL] NVIDIA GeForce RTX 4090
workerpoh: OPENCL calibrated 148.98 GH/s (batch=4194304)
submit ok found=true/false ... ghs=148.982660 mod=...
```

| Metric | Value |
|--------|--------|
| Calibrated GH/s | **148.98** (stable entire run) |
| `submit ok` | 120 |
| `found=true` | 27 |
| HTTP 403 | 0 |
| Batch | 4194304 |
| `target_mod` | 12 373 685 → 24 898 283 (retarget observed) |

Post-run `nvidia-smi` snapshot showed low utilization (miner already stopped by `timeout`); during run logs show sustained ~149 GH/s submit rate.

## Coordinator snapshot (`vast-4090-01`)

- `online`: true
- `hashrate_gh_s` / `peak_hashrate_gh_s`: **148.98**
- `accepted_ranges`: 120
- `accepted_hits`: 27
- `accepted_attempts`: 503 316 480
- `payout_hmc`: ~0.564
- `payout_sup`: ~0.024
- Single worker key (1 GPU — no `-gpu0` split required)

## Fuzz / audit (local node)

With tunnel `BASE=http://127.0.0.1:8080`:

```bash
bash scripts/ops/security_audit_gate.sh
# [audit-gate] PASS — fuzz_report_v2 issues 0
```

Distributed pool fuzz (`pool_fuzz_distributed_gate.sh`) targets a local coordinator DB; use prod only via documented admin APIs if needed.

## Verdict

| Area | Result |
|------|--------|
| HMC mining (OpenCL) | **PASS** — ~149 GH/s, hits + payout |
| Single worker row | **PASS** — `vast-4090-01` only |
| CUDA pack on Vast CUDA 13 | **WARN** — same NVRTC issue as 30/40 series |
| Network | **WARN** — ~238 ms RTT; still acceptable for submits |

## Artifacts

- `reports/vast-remote/rtx4090-ultra/vast-ultra/` — inventory, `worker.log`, `nonce.seq`
- `reports/vast-session/latency-US-4090-RTX4090-*.txt` (on host under pack `reports/vast-session/`)

## Next in series

1. Local **RTX 5060 Ti** (CUDA 13 / Blackwell path)
2. Vast **RTX 5090**
3. AMD OpenCL fleet
4. Final miner pack rebuild (NVRTC13 + optional `HACKME_GPU_FLEET_AGGREGATE_ID` for multi-GPU)
