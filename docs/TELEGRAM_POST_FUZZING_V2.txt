🔬 HackMe Fuzz Engine v2 — current miner channel 0.1.0-rc11k is live

We upgraded the fuzz pipeline (not just more hashrate):

✅ Seed corpus — security-oriented inputs (OOB / div-zero / overflow classes), not only counter 1..N
✅ Bit-flip mutation — 4 rounds per seed (splitmix64 schedule)
✅ Coverage buckets v2 — buckets from real 64-bit input bytes (not input_n % 257)
✅ Artifacts — SHA256(input) + repro file per finding (check(0x…))
✅ Diff++ — findings + coverage delta vs baseline campaign
✅ CLI — hackme-fuzzing campaign create|status (admin on local node)

Same WASM contract: export check(i64)->i32 · sandbox · HTML report + gate.

Reproduce: go test -run TestDeriveFuzzInput · bash scripts/ops/fuzz_engine_v2_gate.sh

📦 Repo: https://github.com/jokeez/hackme
📄 Core 5-module report: https://hackme.tech/reports/bitcoin-core-5module.html
⛏ Pool: https://hackme.tech

#HackMe #Fuzzing #SecurityResearch #UsefulPoW #WASM
