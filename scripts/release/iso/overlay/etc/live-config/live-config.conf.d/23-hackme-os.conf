# HackMe OS live session (casper). Live user: ubuntu (casper 25adduser breaks with username=root).
LIVE_HOSTNAME="hackme-os"
LIVE_CONFIG_CMDLINE="true"
LIVE_AUTOLOGIN="true"
LIVE_AUTOLOGIN_USER="ubuntu"
