[Unit]
Description=HackMe News Bot Watchdog Check
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
# Always invoke via bash — script may lack +x (status=203/EXEC otherwise).
ExecStart=/bin/bash /opt/hackme/scripts/ops/news_bot_healthcheck.sh
Nice=10
